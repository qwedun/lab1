import math
print('Выберите функцию:', 'Сложение', 'Вычитание', 'Умножение', 'Деление', 'Возведение в степень', 'Логарифм', 'Округление в большую сторону до N знака после запятой', 'Округление в меньшую сторону до N знака после запятой', '', sep='\n')
func=str(input())
if func =='Сложение':
    a=float(input('Введите первое число:'))
    b=float(input('Введите второе число:'))
    print(a+b)
if func=='Вычитание':
    a=float(input('Введите число, из которого собираетесь вычитать:'))
    b=float(input('Введите вычитаемое'))
    print(a-b)
if func=='Умножение':
    a=float(input('Введите первый элемент:'))
    b=float(input('Введите второй элемент:'))
    print(a*b)
if func=='Деление':
    a=float(input('Введите число, которое хотите разделить'))
    b=float(input('Введите делитель'))
    if b==0:
        print('На 0 делить нельзя!')
    else:
        print(a/b)
if func==('Возведение в степень'):
    a=float(input('Введите число, которое хотите возвести в степень'))
    b=float(input('Введите степень'))
    print(a**b)
if func==('Логарифм'):
    a=float(input('Введите основание'))
    b=float(input('Введите аргумент'))
    if a<=0:
        print('Основание должно быть больше нуля!')
    else:
        print(math.log(b, (a)))
if func==('Округление в большую сторону до N знака после запятой'):
    a=float(input('Введите число'))
    b=int(input('Введите, до какого по счёту знака после запятой округлять'))
    c=a*(10**b)
    print(math.ceil(c)/(10**b))
if func==('Округление в меньшую сторону до N знака после запятой'):
    a=float(input('Введите число'))
    b=int(input('Введите, до какого по счёту знака после запятой округлять'))
    c=a*(10**b)
    print(math.floor(c)/(10**b))
